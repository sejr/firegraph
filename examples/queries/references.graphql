query {
    posts {
        id
        message
        author(matchesKeyFromCollection: "users") {
            id
            fullName
        }
        comments {
            id
            message
            author(matchesKeyFromCollection: "users") {
                id
                fullName
            }
        }
    }
}