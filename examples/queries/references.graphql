query {
    posts {
        id
        message
        author(fromCollection: "users") {
            id
            fullName
        }
        comments {
            id
            message
            author(fromCollection: "users") {
                id
                fullName
            }
        }
    }
}